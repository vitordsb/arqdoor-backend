# Copy to `.env.test` (this file is gitignored).
#
# Local MySQL via Docker (recommended):
#   docker compose -f docker-compose.test.yml up -d
#
# Then run:
#   npm test
#
NODE_ENV=test

DB_HOST=127.0.0.1
DB_PORT=3308
DB_NAME=arqdoor_test
DB_USER=arqdoor_test
DB_PASSWORD=arqdoor_test

SECRET=change-me-test-secret
ASAAS_API_KEY=test-key
